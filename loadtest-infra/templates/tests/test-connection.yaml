apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loadtest-infra.fullname" . }}-test-connection"
  labels:
    {{- include "loadtest-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "loadtest-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
