apiVersion: v1
kind: Service
metadata:
  name: {{ include "loadtest-infra.fullname" . }}
  labels:
    {{- include "loadtest-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loadtest-infra.selectorLabels" . | nindent 4 }}
